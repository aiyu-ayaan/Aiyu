# MongoDB Configuration
# FOR DOCKER: Use the hostname 'mongodb' (not 'localhost')
# CRITICAL: The password in MONGODB_URI must EXACTLY match MONGO_ROOT_PASSWORD below!
MONGODB_URI=mongodb://admin:CHANGE_THIS_PASSWORD@mongodb:27017/aiyu?authSource=admin
# For local development (non-Docker), use: mongodb://localhost:27017/aiyu

# MongoDB Root Credentials (for Docker Compose)
# WARNING: This password MUST match the password in MONGODB_URI above!
# SECURITY: Use a strong, unique password (minimum 16 characters, mix of letters, numbers, symbols)
# Generate secure password: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
MONGO_ROOT_USERNAME=admin
MONGO_ROOT_PASSWORD=CHANGE_THIS_PASSWORD

# N8N Webhook URL
NEXT_PUBLIC_N8N_WEBHOOK_URL=

# Admin Panel Credentials
# SECURITY: Change these immediately after setup!
# Password requirements: minimum 12 characters, mix of upper/lower case, numbers, symbols
ADMIN_USERNAME=admin
ADMIN_PASSWORD=CHANGE_ME_MINIMUM_12_CHARS

# JWT Secret (CRITICAL - must be strong and unique)
# SECURITY: Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# Minimum 32 characters required, recommended 64+
JWT_SECRET=CHANGE_ME_GENERATE_RANDOM_64_CHAR_STRING

# Blog API Key (for external integrations like n8n)
# SECURITY: Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
BLOG_API_KEY=CHANGE_ME_GENERATE_RANDOM_API_KEY

# Application Port (optional, defaults to 3000)
APP_PORT=3000

# ====================
# SECURITY REMINDERS
# ====================
# 1. NEVER commit the actual .env file to git
# 2. Rotate all secrets regularly (monthly recommended)
# 3. Use different passwords for each environment (dev/staging/prod)
# 4. Keep .env file permissions restrictive (chmod 600 on Linux/Mac)
# 5. Use strong, random secrets - DO NOT use dictionary words
