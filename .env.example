# MongoDB Configuration
# FOR DOCKER: Use the hostname 'mongodb' (not 'localhost')
# CRITICAL: The password in MONGODB_URI must EXACTLY match MONGO_ROOT_PASSWORD below!
MONGODB_URI=mongodb://admin:CHANGE_THIS_PASSWORD@mongodb:27017/aiyu?authSource=admin
# For local development (non-Docker), use: mongodb://localhost:27017/aiyu

# MongoDB Root Credentials (for Docker Compose)
# WARNING: This password MUST match the password in MONGODB_URI above!
# Use a strong, unique password - replace CHANGE_THIS_PASSWORD with the same value in both places
MONGO_ROOT_USERNAME=admin
MONGO_ROOT_PASSWORD=CHANGE_THIS_PASSWORD

# N8N Webhook URL
NEXT_PUBLIC_N8N_WEBHOOK_URL=

# Admin Panel Credentials
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your_secure_password

# JWT Secret (generate a secure random string)
JWT_SECRET=your_jwt_secret_key_here

# Application Port (optional, defaults to 3000)
APP_PORT=3000
